import styles from '../about.module.scss';

export default function aboutPage() {
  return (
    <div className={styles.about}>
      You can't believe your eyes?
      <br />
      But that's how it is, the about page is as empty as it looks.
    </div>
  );
}
